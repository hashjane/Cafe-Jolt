<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tray_id %>
    <%= form.text_field :tray_id, id: :order_tray_id %>
  </div>

  <div class="field">
    <%= form.label :components %>
    <%= form.text_area :components, id: :order_components %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
