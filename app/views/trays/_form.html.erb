<%= form_with(model: tray, local: true) do |form| %>
  <% if tray.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tray.errors.count, "error") %> prohibited this tray from being saved:</h2>

      <ul>
      <% tray.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
