<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<h5>Your Tray:</h5>
<table class="table-condensed">

  <%= render partial: 'line_items/line_item', object: @tray_items %>

</table>

<div class="btn-toolbar">
  <%= button_to "Order", orders_path, method: :post, class: 'btn btn-success' %>
</div>

